<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Password Reset - TamWill</title>
    <style>
        /* Base Reset */
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #000000; /* Black text */
            background-color: #f3f4f6; /* Light gray bg for contrast */
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 600px;
            margin: 40px auto;
            background-color: #ffffff;
            border: 2px solid #000000; /* Thick black border */
            padding: 40px;
        }
        .header {
            text-align: center;
            padding-bottom: 30px;
            border-bottom: 4px solid #000000; /* Bold separator */
            margin-bottom: 30px;
        }
        .logo {
            font-size: 28px;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: -1px;
            color: #000000;
            text-decoration: none;
        }
        .content {
            padding: 0 10px;
        }
        h2 {
            font-size: 24px;
            font-weight: 900;
            text-transform: uppercase;
            margin-top: 0;
            margin-bottom: 20px;
        }
        .button {
            display: inline-block;
            padding: 15px 30px;
            background-color: #000000; /* Black button */
            color: #ffffff !important;
            text-decoration: none;
            font-weight: bold;
            text-transform: uppercase;
            border-radius: 6px;
            margin: 25px 0;
            /* Faking a hard shadow with border since box-shadow is flaky in email */
            border: 2px solid #000000;
        }
        .button:hover {
            background-color: #333333;
        }
        .link-box {
            background-color: #ffffff;
            border: 2px dashed #000000; /* Dashed border style */
            padding: 15px;
            word-break: break-all;
            font-family: monospace;
            font-size: 14px;
            margin: 20px 0;
        }
        .warning {
            background-color: #f9fafb;
            border: 2px solid #000000;
            padding: 15px;
            margin-top: 30px;
            font-size: 13px;
        }
        .footer {
            text-align: center;
            padding-top: 30px;
            margin-top: 30px;
            border-top: 1px solid #eeeeee;
            font-size: 12px;
            color: #666666;
            font-weight: bold;
            text-transform: uppercase;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">TamWill</div>
        </div>

        <div class="content">
            <h2>Reset Password</h2>

            <p><strong>Hello {{ user.name }},</strong></p>

            <p>We received a request to reset your password. If this was you, click the button below to proceed.</p>

            <div style="text-align: center;">
                <a href="{{ resetUrl }}" class="button">Reset My Password</a>
            </div>

            <p style="font-size: 14px; font-weight: bold;">Or copy this link:</p>
            <div class="link-box">
                {{ resetUrl }}
            </div>

            <div class="warning">
                <strong>⚠️ EXPIRATION:</strong> This link expires on {{ expiresAt|date('F j, Y \\a\\t g:i A') }}.
            </div>

            <p style="font-size: 12px; color: #666; margin-top: 20px;">
                If you didn't request this, you can safely ignore this email.
            </p>
        </div>

        <div class="footer">
            <p>Sent to {{ user.email }}</p>
            <p>&copy; {{ 'now'|date('Y') }} TamWill Inc.</p>
        </div>
    </div>
</body>
</html>
